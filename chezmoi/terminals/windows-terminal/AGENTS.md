# chezmoi/terminals/windows-terminal: Windows Terminal 設定

## 編集対象

- `settings.json` -> `%LOCALAPPDATA%/Packages/Microsoft.WindowsTerminal.../LocalState/settings.json`

## 変更ルール

- ペイン移動・分割キーは WezTerm 側と揃える。
- OS 固有プロファイルの既定値変更時は既存運用への影響を確認する。
