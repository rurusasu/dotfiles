# chezmoi/editors/vscode: VS Code 設定

## 編集対象

- `settings.json`
- `keybindings.json`
- `extensions.json`

## 変更ルール

- エディタ共通方針（format on save / tab size / keybinding policy）を維持する。
- 拡張追加時は `extensions.json` と実運用の整合性を保つ。
