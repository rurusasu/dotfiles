nerdfont: true
merge_strategy: rebase

agent: cx

agents:
  cx: "codex --auto-approve"

files:
  symlink:
    - .envrc

post_create:
  - direnv allow

panes:
  - command: <agent>
    focus: true
  - split: horizontal
