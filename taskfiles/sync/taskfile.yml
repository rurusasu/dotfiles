version: "3"

tasks:
  skills:sync:
    desc: Sync shared agent skills from deployed ~/.agents/skills/ back to chezmoi source
    cmds:
      - chezmoi re-add ~/.agents/skills/
    status:
      - chezmoi diff ~/.agents/skills/ | grep -qv .
