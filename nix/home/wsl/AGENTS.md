# AGENTS

Purpose
Purpose: WSL-specific Home Manager entrypoint.
Expected contents:
- default.nix: imports nix/home/users/<user>.nix and nix/profiles/home/common.nix.
Notes:
- Use for WSL-only HM toggles if needed.

