# AGENTS

Purpose: WSL-specific Home Manager entrypoint.
Expected contents:
- default.nix: defines home.username/homeDirectory and imports profiles/home/common.nix.
Notes:
- Use for WSL-only HM toggles if needed.

