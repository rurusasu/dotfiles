# AGENTS

Purpose: Shell configurations (bash, zsh).
Expected contents:
- default.nix: imports bash/ and zsh/.
- bash/: bash configuration files (.bashrc, .profile, .bash_logout).
- zsh/: zsh configuration with shell aliases.
